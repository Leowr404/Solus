using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ground : MonoBehaviour
{
    /*public ModuloPista[] listaModulos;

    public int tamanhoInicial;
    ModuloPista copiado;
    private static Ground pista;
    
   


    public List<ModuloPista> poolModulosPista;

    public float velocidade;
    private Vector3 posicaoPista;
    Vector3 posicao;

    private void MovimentarPista()
    {
        posicaoPista.z -= velocidade;
        this.transform.position = posicaoPista;
    }

    private void GerarPista()
    {
        int ModuloSorteado = Random.Range(1, listaModulos.Length);
        posicao = copiado.connector.transform.position;

        copiado = Instantiate(listaModulos[ModuloSorteado], this.transform);
        copiado.transform.position = posicao;

    }

    
    void Start()
    {
        InvokeRepeating("MovimentarPista", 2, 0.055f);
        posicaoPista = this.transform.position;
       
        pista = this;

        
        copiado = Instantiate(listaModulos[0], this. transform);

        
            GerarPista();
        
    }*/

    
}
